# Redirect www to non-www (canonical URL)
https://www.onlinetextcomparetool.netlify.app/* https://onlinetextcomparetool.netlify.app/:splat 301!

# Serve sitemap.xml directly
/sitemap.xml  /sitemap.xml  200

# Handle client-side routing in React
/*            /index.html   200
